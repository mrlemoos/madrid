/**
 * Fallback for the `@footer` slot when Next can't recover a matching footer for the
 * active route (required for parallel routes). Renders nothing.
 */
export default function AuthFooterDefault(): null {
  return null;
}
