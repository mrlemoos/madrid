/** Shared face for signed-out display type (epigraph + auth card titles). */
export const bricolageGrotesqueStyle = {
  fontFamily:
    '"Bricolage Grotesque Variable", "Bricolage Grotesque", ui-sans-serif, system-ui, sans-serif',
} as const;
